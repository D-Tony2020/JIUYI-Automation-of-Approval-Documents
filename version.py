# -*- coding: utf-8 -*-
"""版本信息 — 版本相关常量的唯一来源(GUI/打包/about 都引此)。"""

VERSION = "0.3.1"
APP_NAME = "久益-承认书自动化"
BUILD_DATE = "2026-06-29"

# v0.3.1:
#   - API key 改 UI 填写: 取消强制环境变量, 首页「⚙ API设置」直接填+随包预填, 开箱即用; 解析 配置→随包默认→环境变量。
#   - ★根治 OneDrive 重定向 %APPDATA% 上 os.replace 跨设备(WinError17)致【学习库/配置静默不持久化】: 统一走 userdata.atomic_write_json 兜底直写。
#   - 理材质溯源标记: 字典推断的字段(标准名/类别/零件/成份名/CAS)标琥珀"荐·待核对", 区别 MSDS 直接提取的事实; 编辑/标记通过即转正常; CAS↔名实时自动带。
#   - 成份知识库: 材质成份库(操作员增删自动跟踪) + CAS规范库(双向 7440-50-8↔铜(Cu)); 端子(磷铜)文件名兜底+源链接按钮。
#   - 修 新增/拖入材质未继承零件级供应商致"挂靠后仍显示未选择供应商"死结: bomstate.syncPartSuppliers 自愈不变式。
# v0.3.0:
#   - 成长学习无监督命中: 人工归位知识(目标列/横排槽/排除+材质+零件)固化, 下次同名/料号文件自动落位不再打扰人(强键防串味, 库可清理)。
#   - ④挂文件卡片范式: 部件承认书/UL/信赖性→卡片(槽=零件, 可增删改) + 包装/出货/图纸单槽卡; 待归位池纯拖拽(退役下拉), 拖到排除区=本单不收录(留痕)。
# v0.2.0:
#   - 文件零丢失覆盖: 每个上传文件都有主(全集=已放置∪待归位∪显式排除, 覆盖不变式可断言)。
#     未识别(route=∅)/认不准/豁免材质的文件 全部进④"待归位池", 可挂到 材质K/L/Y列 或 横排槽(部件/UL/信赖性)+零件 或 本单不收录。
#     红线: 材质豁免只免BOM行, 其文件不丢; 放置honor手动归位; 软门强提示未归位数, 不静默漏件。
# v0.1.0 首个打包版:
#   - 5步流(核对图纸→理材质→挂文件→照片→导出)完整跑通, 全本承认书含OLE一键产出。
#   - 材料文件池(materials/ 直拖=UI上传 + 实时跟踪) + 手动加零件 + 人工修改审计留痕。
#   - 成长型字典学习(简称/类别零件/供应商/零件序/归属); OLE原文件名嵌入 + 材质证明零件标签。
#   - 持久化: 字典学习库存 %APPDATA%\\Moore\\久益-承认书自动化, 重装/升级继承(参考久益-标签自动化)。
