# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Excel 临时锁文件
~$*.xlsx
~$*.xls

# 中间产物留档（保留在磁盘供随时调取，不入 git 以免膨胀）
产出留档/
.work/

# 本单输入（含生久图纸=客户 IP，按安全红线不入 git；run_w3 按需从 golden 重抽）
本单输入/

# API key（密钥不入库=安全红线；本地存在、随 exe 打包，但永不提交 git）
hitl/data/_api_key.txt
config.json

# OS
.DS_Store
Thumbs.db

# PyInstaller 打包产物
/build/
/dist/
*.log
